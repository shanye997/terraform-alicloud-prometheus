# With Existing Prometheus Alerting

This example creates a Prometheus instance outside the module, then configures an email contact, contact group, and dispatch rule through the module.

Run:

```shell
terraform init
terraform plan
terraform apply
```
