# With Collection

This example creates VPC networking and a Prometheus instance outside the module, then creates an ARMS ECS environment and its custom scrape job through the module.

Run:

```shell
terraform init
terraform plan
terraform apply
```
